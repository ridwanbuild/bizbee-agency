/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'tse2.mm.bing.net',
      't3.ftcdn.net',
      'www.unitopled.com',
      'img.freepik.com'
    ],
  },
}

module.exports = nextConfig;
